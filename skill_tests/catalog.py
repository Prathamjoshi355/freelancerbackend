SKILL_BLUEPRINTS = {
    "react": {
        "name": "React",
        "category": "frontend",
        "description": "Component-driven UI engineering for production web apps.",
        "concepts": [
            {"term": "useState", "purpose": "manage local component state between renders", "scenario": "a form or toggle needs to update and re-render the UI", "tradeoff": "too many unrelated state values in one component reduce clarity", "best_practice": "group state by behavior instead of creating arbitrary flags", "distractors": ["compile JSX into SQL", "store API secrets in props", "replace routing with CSS"]},
            {"term": "useEffect", "purpose": "synchronize a component with external systems after render", "scenario": "subscriptions, timers, or API sync must follow UI lifecycle changes", "tradeoff": "bad dependency lists can create stale values or repeated side effects", "best_practice": "keep effects focused and declare every reactive dependency they read", "distractors": ["run database migrations in the browser", "replace events with templates", "sign JWTs inside CSS"]},
            {"term": "props", "purpose": "pass data and behavior from parent components to children", "scenario": "a shared project card must render different job data in multiple screens", "tradeoff": "deep prop chains become noisy when too many layers forward the same value", "best_practice": "keep component interfaces explicit and small", "distractors": ["persist passwords to component markup", "replace API calls with images", "store routing state in colors"]},
            {"term": "keys", "purpose": "help React track list items across re-renders", "scenario": "job feeds or bid tables are filtered, reordered, or appended dynamically", "tradeoff": "unstable keys can make state jump between list items", "best_practice": "use stable identifiers from the underlying data instead of array indexes", "distractors": ["hide network requests from DevTools", "replace auth with local variables", "compile forms into tokens"]},
            {"term": "context", "purpose": "share values across a subtree without manual prop threading", "scenario": "authentication state or theme data is needed in many nested components", "tradeoff": "broad contexts can trigger unnecessary re-renders", "best_practice": "scope providers narrowly and split unrelated values", "distractors": ["replace API retries with gradients", "convert route params into cookies", "store images in JWT claims"]},
            {"term": "memoization", "purpose": "avoid expensive recalculation or child renders when inputs are unchanged", "scenario": "large dashboards or tables re-render frequently", "tradeoff": "blind memoization adds complexity without measurable benefit", "best_practice": "profile first and memoize only real hot paths", "distractors": ["disable dependency tracking globally", "move every function server-side automatically", "replace form validation with cache keys"]},
            {"term": "error boundaries", "purpose": "contain render failures so one bad widget does not crash the whole UI", "scenario": "an unstable analytics card should fail safely inside a dashboard shell", "tradeoff": "they do not catch async event-handler errors on their own", "best_practice": "wrap risky sections with fallback UI and logging", "distractors": ["retry database writes in CSS", "store secrets in fallback components", "replace loading states with payment popups"]},
        ],
        "practical": [
            {"prompt": "Build a verified freelancer dashboard in React with onboarding progress, locked actions, and role-aware navigation.", "keywords": ["state", "progress", "role", "guard", "dashboard"]},
            {"prompt": "Design a bid submission flow that validates form data, prevents duplicate submission, and handles API errors cleanly.", "keywords": ["form", "validation", "error", "submit", "api"]},
            {"prompt": "Create a job chat UI that shows moderation warnings and disables sending blocked messages.", "keywords": ["chat", "message", "warning", "disabled", "moderation"]},
            {"prompt": "Refactor a large marketplace page into reusable React components without breaking interaction state.", "keywords": ["component", "state", "reuse", "refactor", "interaction"]},
        ],
    },
    "django": {
        "name": "Django",
        "category": "backend",
        "description": "Structured backend development with strong validation and APIs.",
        "concepts": [
            {"term": "APIView", "purpose": "define explicit request handling for API endpoints", "scenario": "an endpoint needs custom permission checks and response shaping", "tradeoff": "more control means more manual code than generic views", "best_practice": "use APIView when custom behavior is central to the endpoint", "distractors": ["replace serializers with comments", "encrypt CSS with migrations", "store middleware in images"]},
            {"term": "serializers", "purpose": "validate and transform request and response payloads", "scenario": "registration data must be checked before creating a user", "tradeoff": "putting too much orchestration in serializers blurs responsibilities", "best_practice": "keep payload validation near the edge and workflow rules in services or policies", "distractors": ["compile JWTs into templates", "skip validation for speed", "replace models with routers"]},
            {"term": "permissions", "purpose": "control which users can access an endpoint or action", "scenario": "only hired participants should reach a job chat endpoint", "tradeoff": "permission logic scattered across views is hard to audit", "best_practice": "centralize repeated marketplace rules", "distractors": ["define permissions in CSS", "store ACLs only in localStorage", "replace auth checks with cache headers"]},
            {"term": "middleware", "purpose": "process requests and responses globally around the view layer", "scenario": "cross-cutting security or audit behavior must run for many endpoints", "tradeoff": "heavy middleware can hide control flow and slow every request", "best_practice": "reserve middleware for global concerns and keep domain logic out of it", "distractors": ["write indexes during template rendering", "hash passwords inside static files", "move JWT secrets to query params"]},
            {"term": "querysets", "purpose": "represent database lookups that can be filtered and composed lazily", "scenario": "jobs must be filtered by status, skill, and budget", "tradeoff": "careless chaining can hide performance problems", "best_practice": "shape queries intentionally and measure expensive paths", "distractors": ["convert querysets directly into cookies", "replace pagination with template tags", "store ORM results in CSS variables"]},
            {"term": "transactions", "purpose": "keep related writes consistent when a workflow must succeed or fail together", "scenario": "payment verification and contract updates must stay aligned", "tradeoff": "long transactions can increase lock contention", "best_practice": "keep transaction scopes tight around the critical write set", "distractors": ["wrap static file collection in transactions", "replace retries with comments", "store rollbacks in JWT payloads"]},
            {"term": "throttling", "purpose": "limit abusive request patterns and protect critical endpoints", "scenario": "login and registration endpoints need brute-force protection", "tradeoff": "thresholds that are too low frustrate real users", "best_practice": "apply stricter throttles to abuse-prone actions than to normal reads", "distractors": ["throttle only successful responses", "rate-limit CSS instead of auth", "store request counts in meta tags"]},
        ],
        "practical": [
            {"prompt": "Implement a Django API flow that blocks bidding until profile completion and verified skill tests are satisfied.", "keywords": ["permission", "profile", "skill", "bid", "validation"]},
            {"prompt": "Design a secure registration endpoint with duplicate-face rejection and JWT issuance.", "keywords": ["registration", "jwt", "face", "duplicate", "security"]},
            {"prompt": "Build a job hire transaction that closes a job, hires one bid, and auto-rejects all others.", "keywords": ["job", "hire", "reject", "transaction", "status"]},
            {"prompt": "Create a DRF moderation endpoint that blocks off-platform payment attempts in chat messages.", "keywords": ["moderation", "chat", "payment", "block", "regex"]},
        ],
    },
    "python": {
        "name": "Python",
        "category": "backend",
        "description": "General-purpose programming for automation and platform logic.",
        "concepts": [
            {"term": "list comprehensions", "purpose": "create derived lists concisely from iterables", "scenario": "filtering passed skills into a response payload", "tradeoff": "overly dense comprehensions reduce readability", "best_practice": "use them for clear transformations and switch to loops when logic grows", "distractors": ["replace functions with CSS", "store secrets in indices", "turn exceptions into env vars"]},
            {"term": "generators", "purpose": "produce values lazily without building the whole result in memory", "scenario": "streaming large exports or iterating expensive pipelines", "tradeoff": "debugging long generator chains can be harder than lists", "best_practice": "prefer generators when data can be consumed incrementally", "distractors": ["guarantee thread safety automatically", "encrypt MongoDB collections", "skip error handling globally"]},
            {"term": "decorators", "purpose": "wrap functions or methods with reusable behavior", "scenario": "auth or audit logic must apply consistently to many call sites", "tradeoff": "too many stacked decorators can obscure control flow", "best_practice": "use decorators for repeated cross-cutting behavior with clear intent", "distractors": ["replace type hints with routes", "compile venvs into wheels automatically", "store tests in decorator args"]},
            {"term": "context managers", "purpose": "ensure setup and cleanup happen reliably around a block of code", "scenario": "opening files, locks, or network resources that must always be released", "tradeoff": "custom context managers add indirection if lifecycle is trivial", "best_practice": "use them where resource cleanup must be deterministic", "distractors": ["convert syntax errors into warnings", "replace exceptions with return codes", "store file handles in JWT claims"]},
            {"term": "exceptions", "purpose": "signal and handle failure paths explicitly", "scenario": "invalid payment verification must stop contract changes", "tradeoff": "broad exception swallowing hides production bugs", "best_practice": "catch only the failures you can handle meaningfully", "distractors": ["skip logging because errors are expected", "replace validation with crashes", "store exceptions in CSS variables"]},
            {"term": "type hints", "purpose": "document expected data shapes and improve tooling feedback", "scenario": "service functions return structured workflow and rating data", "tradeoff": "incorrect hints create false confidence", "best_practice": "treat types as living documentation", "distractors": ["enforce indexes automatically", "replace tests fully", "serialize uploads into annotations"]},
            {"term": "unit tests", "purpose": "verify small pieces of logic with deterministic expectations", "scenario": "rating calculations and moderation rules need regression protection", "tradeoff": "coverage without meaningful assertions adds little value", "best_practice": "target business-critical branches and edge cases first", "distractors": ["skip integration concerns forever", "replace review automatically", "store results in env vars"]},
        ],
        "practical": [
            {"prompt": "Write Python service logic that calculates freelancer access state from profile, skills, and review history.", "keywords": ["state", "profile", "skill", "review", "logic"]},
            {"prompt": "Implement a robust content moderation helper that detects emails, phone numbers, and off-platform payment terms.", "keywords": ["regex", "content", "moderation", "payment", "phone"]},
            {"prompt": "Build a deterministic grading helper for MCQ and practical submissions.", "keywords": ["grading", "score", "mcq", "practical", "helper"]},
            {"prompt": "Refactor duplicated platform rules into testable Python functions shared by multiple API views.", "keywords": ["refactor", "shared", "function", "rule", "testable"]},
        ],
    },
    "mongodb": {
        "name": "MongoDB",
        "category": "database",
        "description": "Document database design for flexible, high-throughput applications.",
        "concepts": [
            {"term": "documents", "purpose": "store related data together as structured BSON records", "scenario": "a profile keeps onboarding and rating fields close together", "tradeoff": "deep nesting can become awkward to update", "best_practice": "model documents around access patterns", "distractors": ["replace indexes with CSS", "store sockets in collections", "skip schema thinking entirely"]},
            {"term": "collections", "purpose": "group related documents of the same general shape", "scenario": "jobs, bids, payments, and messages each live in separate collections", "tradeoff": "too many tiny collections complicate discovery", "best_practice": "split collections by domain boundaries and query behavior", "distractors": ["treat collections as sessions", "store logs in JWT claims", "replace permissions with collection names"]},
            {"term": "indexes", "purpose": "speed up common query patterns by organizing searchable fields", "scenario": "email lookup and open-job filters must stay fast as data grows", "tradeoff": "too many indexes slow writes and consume storage", "best_practice": "add indexes for proven read patterns and review cost", "distractors": ["guarantee validity without validation", "replace backups automatically", "store images in index definitions"]},
            {"term": "embedding", "purpose": "keep tightly related data together in one document", "scenario": "question snapshots are stored directly on a test attempt", "tradeoff": "embedded data is harder to reuse across unrelated queries", "best_practice": "embed when child data is read with the parent almost every time", "distractors": ["embed secrets in public profiles", "replace replication with arrays", "store retries inside documents"]},
            {"term": "referencing", "purpose": "link documents when relationships need independent lifecycle management", "scenario": "a bid references both a freelancer and a job", "tradeoff": "extra lookups may be needed during reads", "best_practice": "reference entities that are reused or grow independently", "distractors": ["reference CSS classes across collections", "replace joins with scans", "store passwords as comment refs"]},
            {"term": "aggregation", "purpose": "process and summarize data through pipeline stages", "scenario": "analytics needs grouped counts by status or skill", "tradeoff": "large pipelines can become difficult to reason about", "best_practice": "keep pipelines readable and benchmark heavy stages", "distractors": ["replace API validation with aggregation", "store layouts in pipelines", "guarantee ACID everywhere"]},
            {"term": "atomic updates", "purpose": "change a document safely in one operation", "scenario": "incrementing counters or violation counts without race conditions", "tradeoff": "cross-document workflows still need higher-level coordination", "best_practice": "use atomic operators for counters and state transitions where possible", "distractors": ["replace business rules entirely", "store transactions inside projections", "avoid error handling because updates are atomic"]},
        ],
        "practical": [
            {"prompt": "Model a MongoDB schema for jobs, bids, contracts, and payments with clear indexes and references.", "keywords": ["schema", "index", "reference", "job", "payment"]},
            {"prompt": "Design a face-embedding collection and duplicate-check query strategy for registration.", "keywords": ["embedding", "duplicate", "registration", "query", "collection"]},
            {"prompt": "Create a query plan for listing active freelancers by verified skill and rating.", "keywords": ["query", "freelancer", "rating", "skill", "verified"]},
            {"prompt": "Refactor a content-heavy attempt record to balance embedding and referencing for efficient reads.", "keywords": ["embedding", "referencing", "attempt", "reads", "design"]},
        ],
    },
    "ui-ux": {
        "name": "UI/UX Design",
        "category": "design",
        "description": "Interface and experience design for guided, high-trust workflows.",
        "concepts": [
            {"term": "user flows", "purpose": "map the sequence of actions a person takes to complete a goal", "scenario": "new users move from registration to gated marketplace access", "tradeoff": "missing edge cases creates confusing dead ends", "best_practice": "design flows around real goals and enforced rules", "distractors": ["replace layouts with routes", "turn wireframes into schemas", "skip state-based transitions"]},
            {"term": "wireframes", "purpose": "explore structure and hierarchy before visual polish", "scenario": "a hiring dashboard needs agreement on layout before final styling", "tradeoff": "staying low fidelity too long can delay decisions", "best_practice": "use wireframes to validate structure quickly", "distractors": ["use wireframes as migrations", "replace accessibility testing with grayscale", "store secrets in design files"]},
            {"term": "design systems", "purpose": "provide reusable patterns, tokens, and components across the product", "scenario": "auth, dashboard, and chat should feel consistent without duplication", "tradeoff": "a rigid system can slow necessary product-specific solutions", "best_practice": "balance consistency with purposeful exceptions", "distractors": ["replace rules with color tokens", "use one component for every interaction", "skip documentation because components exist"]},
            {"term": "accessibility", "purpose": "make interfaces usable by people with varied abilities and assistive technologies", "scenario": "payment and moderation actions need clear labels and keyboard support", "tradeoff": "retrofitting accessibility later is more expensive", "best_practice": "treat semantics and feedback states as core design", "distractors": ["hide focus states", "replace labels with placeholders only", "disable error messages"]},
            {"term": "visual hierarchy", "purpose": "guide attention to the most important information first", "scenario": "onboarding blockers should stand out above secondary analytics", "tradeoff": "too many competing highlights make everything feel equally urgent", "best_practice": "make the next required action visually obvious", "distractors": ["use identical weight for all text", "place warnings below unrelated content", "hide primary actions until hover"]},
            {"term": "responsive design", "purpose": "adapt layout and interaction patterns across screen sizes", "scenario": "job posting and chat must work on mobile and desktop", "tradeoff": "desktop-heavy assumptions create cramped mobile workflows", "best_practice": "design key actions for small screens first when they are workflow critical", "distractors": ["ship desktop-only tables everywhere", "replace navigation with zoom instructions", "skip touch target sizing"]},
            {"term": "information architecture", "purpose": "organize content so users can find and understand what they need", "scenario": "clients need jobs, bids, contracts, payments, and chat to feel connected", "tradeoff": "poor grouping increases cognitive load", "best_practice": "organize screens around user goals and state transitions", "distractors": ["group pages only by stack layer", "hide related actions across random menus", "store IA decisions in env vars"]},
        ],
        "practical": [
            {"prompt": "Design a multi-step onboarding UI that clearly communicates locked marketplace actions until verification is complete.", "keywords": ["onboarding", "step", "locked", "verification", "ui"]},
            {"prompt": "Create a client hiring view that compares bids, trust signals, and skill ratings without overwhelming the user.", "keywords": ["client", "bid", "rating", "compare", "trust"]},
            {"prompt": "Design a chat interface that blocks off-platform messages while keeping the conversation understandable and recoverable.", "keywords": ["chat", "warning", "blocked", "recoverable", "message"]},
            {"prompt": "Build a dashboard visual hierarchy where next-step actions dominate secondary statistics.", "keywords": ["dashboard", "hierarchy", "action", "statistics", "next-step"]},
        ],
    },
}
